services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - /mnt/mediapool/movies:/movies
      - /mnt/mediapool/tvshows:/tvshows
    restart: unless-stopped
